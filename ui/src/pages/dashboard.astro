---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Mi Dashboard - RSVP">
  <main class="min-h-screen bg-gradient-to-br from-rose-50 to-pink-100 py-8">
    <div class="container mx-auto px-4 max-w-4xl">
      <!-- Header -->
      <div class="bg-white rounded-2xl shadow-xl p-8 mb-8">
        <div class="text-center">
          <div class="text-6xl mb-4">👋</div>
          <h1 class="text-3xl font-bold text-gray-800 mb-2">¡Bienvenido!</h1>
          <p class="text-gray-600" id="welcome-message">Cargando tu información...</p>
        </div>
      </div>

      <!-- Estado del RSVP -->
      <div class="grid md:grid-cols-2 gap-8">
        <!-- Mi RSVP -->
        <div class="bg-white rounded-2xl shadow-lg p-6">
          <h2 class="text-2xl font-bold text-gray-800 mb-4 flex items-center">
            <span class="text-2xl mr-2">📝</span>
            Mi RSVP
          </h2>
          
          <div id="rsvp-status" class="space-y-4">
            <div class="animate-pulse">
              <div class="h-4 bg-gray-200 rounded w-3/4 mb-2"></div>
              <div class="h-4 bg-gray-200 rounded w-1/2"></div>
            </div>
          </div>
          
          <div class="mt-6 space-y-3">
            <a 
              href="/rsvp/form" 
              class="block w-full bg-gradient-to-r from-rose-500 to-pink-500 text-white py-3 px-6 rounded-lg text-center font-semibold hover:from-rose-600 hover:to-pink-600 transition-all duration-200"
            >
              ✏️ Editar mi RSVP
            </a>
            
            <button 
              id="download-summary"
              class="w-full bg-purple-500 text-white py-3 px-6 rounded-lg font-medium hover:bg-purple-600 transition-colors"
            >
              📄 Descargar Resumen
            </button>
          </div>
        </div>

        <!-- Información del evento -->
        <div class="bg-white rounded-2xl shadow-lg p-6">
          <h2 class="text-2xl font-bold text-gray-800 mb-4 flex items-center">
            <span class="text-2xl mr-2">📅</span>
            Información del Evento
          </h2>
          
          <div class="space-y-4">
            <div class="bg-rose-50 rounded-lg p-4">
              <h3 class="font-semibold text-gray-800 mb-2">Ceremonia</h3>
              <p class="text-sm text-gray-600">Sábado, 15 de Junio 2024</p>
              <p class="text-sm text-gray-600">17:00h - Iglesia San José</p>
            </div>
            
            <div class="bg-purple-50 rounded-lg p-4">
              <h3 class="font-semibold text-gray-800 mb-2">Celebración</h3>
              <p class="text-sm text-gray-600">18:00h - Palacio de Eventos</p>
              <p class="text-sm text-gray-600">Cóctel, cena y fiesta</p>
            </div>
            
            <div class="bg-blue-50 rounded-lg p-4">
              <h3 class="font-semibold text-gray-800 mb-2">Recordatorio</h3>
              <p class="text-sm text-gray-600">Confirma antes del 1 de Mayo</p>
              <p class="text-sm text-gray-600">Dress code: Elegante</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Acciones adicionales -->
      <div class="mt-8 bg-white rounded-2xl shadow-lg p-6">
        <h2 class="text-2xl font-bold text-gray-800 mb-4 text-center">
          🔗 Enlaces Útiles
        </h2>
        
        <div class="grid md:grid-cols-3 gap-4">
          <a 
            href="/" 
            class="bg-gray-50 hover:bg-gray-100 rounded-lg p-4 text-center transition-colors"
          >
            <div class="text-2xl mb-2">🏠</div>
            <p class="font-medium text-gray-800">Página Principal</p>
            <p class="text-sm text-gray-600">Ver toda la información</p>
          </a>
          
          <a 
            href="/#location" 
            class="bg-gray-50 hover:bg-gray-100 rounded-lg p-4 text-center transition-colors"
          >
            <div class="text-2xl mb-2">📍</div>
            <p class="font-medium text-gray-800">Ubicación</p>
            <p class="text-sm text-gray-600">Cómo llegar</p>
          </a>
          
          <button 
            id="logout-btn"
            class="bg-gray-50 hover:bg-gray-100 rounded-lg p-4 text-center transition-colors"
          >
            <div class="text-2xl mb-2">🚪</div>
            <p class="font-medium text-gray-800">Cerrar Sesión</p>
            <p class="text-sm text-gray-600">Salir de mi cuenta</p>
          </button>
        </div>
      </div>
    </div>
  </main>
</BaseLayout>

<script>
  document.addEventListener('DOMContentLoaded', async () => {
    try {
      // Verificar autenticación
      const authResponse = await fetch('http://localhost:3001/auth/me', {
        credentials: 'include'
      });
      
      if (!authResponse.ok) {
        window.location.href = '/rsvp/login';
        return;
      }
      
      const authData = await authResponse.json();
      if (!authData.success) {
        window.location.href = '/rsvp/login';
        return;
      }
      
      // Mostrar información del usuario
      const welcomeMessage = document.getElementById('welcome-message');
      if (welcomeMessage) {
        welcomeMessage.textContent = `Hola ${authData.user.name}, aquí tienes tu información de RSVP`;
      }
      
      // Cargar estado del RSVP
      const rsvpResponse = await fetch('http://localhost:3001/api/rsvp/form', {
        credentials: 'include'
      });
      
      if (rsvpResponse.ok) {
        const rsvpData = await rsvpResponse.json();
        if (rsvpData.success) {
          displayRSVPStatus(rsvpData.form);
        }
      }
      
    } catch (error) {
      console.error('Error loading dashboard:', error);
      window.location.href = '/rsvp/login';
    }
  });

  function displayRSVPStatus(rsvp: any) {
    const statusContainer = document.getElementById('rsvp-status');
    if (!statusContainer) return;
    
    let statusHtml = '';
    
    if (rsvp.attending === null) {
      statusHtml = `
        <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
          <p class="font-semibold text-yellow-800">⏳ Pendiente de confirmación</p>
          <p class="text-sm text-yellow-600">Aún no has confirmado tu asistencia</p>
        </div>
      `;
    } else if (rsvp.attending) {
      statusHtml = `
        <div class="bg-green-50 border border-green-200 rounded-lg p-4">
          <p class="font-semibold text-green-800">✅ ¡Confirmado!</p>
          <p class="text-sm text-green-600">Gracias por confirmar tu asistencia</p>
          ${rsvp.plus_one ? `<p class="text-sm text-green-600">+ Acompañante: ${rsvp.plus_one_name || 'Sin especificar'}</p>` : ''}
        </div>
      `;
    } else {
      statusHtml = `
        <div class="bg-red-50 border border-red-200 rounded-lg p-4">
          <p class="font-semibold text-red-800">❌ No podrás asistir</p>
          <p class="text-sm text-red-600">Lamentamos que no puedas acompañarnos</p>
        </div>
      `;
    }
    
    // Agregar detalles adicionales si existen
    if (rsvp.menu_choice || rsvp.dietary_restrictions || rsvp.notes) {
      statusHtml += `
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mt-4">
          <p class="font-semibold text-blue-800 mb-2">📋 Detalles adicionales:</p>
          ${rsvp.menu_choice ? `<p class="text-sm text-blue-600">Menú: ${rsvp.menu_choice}</p>` : ''}
          ${rsvp.dietary_restrictions ? `<p class="text-sm text-blue-600">Restricciones: ${rsvp.dietary_restrictions}</p>` : ''}
          ${rsvp.notes ? `<p class="text-sm text-blue-600">Notas: ${rsvp.notes}</p>` : ''}
        </div>
      `;
    }
    
    statusContainer.innerHTML = statusHtml;
  }

  // Logout
  document.getElementById('logout-btn')?.addEventListener('click', async () => {
    try {
      await fetch('http://localhost:3001/auth/logout', {
        method: 'POST',
        credentials: 'include'
      });
      window.location.href = '/';
    } catch (error) {
      console.error('Error logging out:', error);
    }
  });

  // Download summary
  document.getElementById('download-summary')?.addEventListener('click', () => {
    alert('Función de descarga en desarrollo. ¡Próximamente disponible!');
  });
</script>